# Rust guidance

Read `.codex/languages/typescript.md` for the shared Tauri layout and complete repository quality gate, including for Rust-only shell changes. It runs the Rust checks against `src-tauri/Cargo.toml`.
