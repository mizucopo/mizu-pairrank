# mizu-pairrank

## Development

Install dependencies and start the Tauri development app:

```bash
npm ci
npm run tauri dev
```

Run the full quality check before handing off changes:

```bash
npm run check
```

## TypeScript toolchain

The project uses TypeScript 6 so the compiler and `typescript-eslint` share the
same supported compiler API.
