# mizu-pairrank

ふたつの項目を比べて、自分の好みのランキングを作るデスクトップアプリです。

## 使い方

1. リストを作成し、項目名を追加します。改行区切りで一括登録できます。
2. 必要に応じて、項目の「画像」からファイル登録・ネット検索・画像なしを選びます。検索用APIキーを設定すると、開いているリストの画像未登録項目を一括登録できます。
3. 「比較する」で、Aが大好き / Aが好き / 同じ / Bが好き / Bが大好きのいずれかを回答します。キーボードの1〜5でも回答できます。
4. 「ランキング」で現在の順位を確認します。条件を満たすと「順位ほぼ確定」に切り替わり、「比較を続ける」で再開できます。
5. 「Tier 表」で、評価に応じて S・A・B・C・D に分けた項目を確認します。同じ Tier の中では左ほど上位です。

「大好き」は相手との好みの差が大きいことを意味します。5段階へ拡張したTrueSkillで評価と不確実性σを更新し、期待情報量の大きいペアを自動選択します。順位は推定評価μの順です。

Tier はリスト内の最高μから最低μまでを5等分して自動で決まり、境界上の値は下位の Tier に入ります。同じμの項目は同じ Tier に入り、全項目が同点なら C に入ります。比較で評価が変わると Tier も更新されます。

終了条件は、全項目のσが2.5以下、かつ直近max(20,項目数)回答の間に各項目の順位幅が1位以内になることです。新しい項目の追加・削除・比較再開で終了判定の履歴窓をリセットし、既存の評価は保持します。項目数に固定上限はありません。通常500項目未満を想定しています。

## 画像検索

「検索設定」にBrave Search APIまたはOllama Web SearchのAPIキーを登録します。両方登録して検索時に切り替えることもできます。

- Brave: [APIダッシュボード](https://api-dashboard.search.brave.com/)でキーを取得します。画像検索の候補を表示します。
- Ollama: [APIキー設定](https://ollama.com/settings/keys)でキーを取得します。検索結果のWebページから代表画像を取得します。

APIキーはOSの資格情報ストアに保存します。検索には各サービスの料金・利用制限が適用されます。画像候補が取れないページは除外します。APIキーがなくても、手動登録・画像なしで利用できます。

画像の一括登録は、開いているリストの画像未登録項目を対象にします。BraveとOllamaの両方を設定している場合は開始時に検索元を選び、片方だけなら設定済みの検索元を使います。どちらも未設定の場合は一括登録を開始できません。各項目では検索結果の上位から保存できる画像を自動で選び、候補がない項目は画像なしのまま次へ進みます。進捗を確認しながら途中で停止でき、登録した画像は項目の「画像」から手動で変更できます。一括検索でも項目ごとにAPIの利用量が発生します。

手動登録はPNG・JPEG・WebPに対応します。最大20MiB・32メガピクセルまで受け入れ、長辺1600px以下のPNGとしてアプリ内へ保存します。元ファイルは変更しません。取得済み画像はオフラインで表示できます。

## 保存とアップデート

OSの「ドキュメント」内の `mizu-pairrank` フォルダに `pairrank.sqlite3` と `images` ディレクトリを保存します。macOSの通常の保存先は `~/Documents/mizu-pairrank/` です。複数リストの項目・比較履歴・評価・終了判定の履歴は自動保存されます。

保存先がなければ作成して空の状態から開始し、既に保存データがあれば読み込みます。旧アプリデータ領域のデータは自動で移行・削除しません。ドキュメントの場所を取得できない場合や保存先を作成できない場合は、起動エラーを表示して保存データの読み込みを停止します。別の保存先には切り替えません。

macOSでは、復元した保存先に読み取り権限がなく安全に開けない場合、自動で権限を変更せず保存データの読み込みを停止します。保存先の所有者・読み取り権限を確認してから再起動してください。

DBのスキーマバージョンを保持し、起動時に必要なマイグレーションを順番に自動適用します。変更はまとめて確定し、失敗した場合は更新前のスキーマ・データ・バージョンを維持します。新しいDBを古いアプリで開いた場合は、アプリの更新を案内して操作を停止します。

開発時のスキーマ変更手順は[SQLiteと起動時マイグレーション](docs/adr/0002-sqlite-migrations.md)、評価の仕様は[5段階TrueSkill](docs/adr/0001-ordinal-trueskill.md)を参照してください。

### バックアップと復元

同じ端末・同じ保存先への、アプリ停止中のバックアップと復元に対応します。

1. アプリをすべて終了し、同じDBを使う他のツールと、保存先を変更する外部同期も停止します。コピー・復元が完了するまで起動しないでください。
2. バックアップでは、保存先フォルダーを丸ごと、その外にある別のフォルダーへコピーします。`pairrank.sqlite3`・`images`・残っている `-journal`・`-wal`・`-shm` を一式で保存し、副ファイルを個別に削除しないでください。
3. 復元では、現在の保存先フォルダーを別名で退避し、バックアップ一式を元の場所・名前へコピーしてから起動します。既存ファイルへの上書き・混在は避け、リスト・比較履歴・ランキング・画像を確認するまで退避分を残してください。

利用中の保存先をクラウド同期の対象にしないでください。稼働中の直接同期や、DB・副ファイルの個別置換には対応しません。DBと副ファイルの取り違えによる破損や、副ファイル名へ置換した無関係なファイルの削除が起こり得ます。

検索用APIキーはOSの資格情報ストアにあり、このバックアップには含まれません。対応範囲の判断は[保存先とバックアップの方針](docs/adr/0004-pinned-storage.md)を参照してください。

## Development

```bash
npm ci
npm run tauri dev
```

`npm run dev` 単体ではViteのみが起動するため、保存・比較にはTauriから起動してください。

全品質チェック:

```bash
npm run check
```

TypeScript 7・type-aware Oxlint・Prettier・Vitest・Viteビルドと、Rustのfmt・Clippy・テストを実行します。画像検索のテストはモックを利用し、実APIキーやOSの資格情報ストアにはアクセスしません。
